interface:
  display_name: "Beads"
  short_description: "Project task tracking with bd"
  default_prompt: "Use $beads only when durable project-task coordination is part of the task, not for routine repository work merely because bd is installed."
