import { FamiliarRite } from "@/components/familiar-rite";

/** Standalone host for the new rite while it is being designed (cave-3rz.3).
 *  Kept off the shipped navigation so the existing Summoning Circle stays the
 *  supported path until this replaces it. */
export default function RitePage() {
  return <FamiliarRite />;
}
