Third CI defect found+fixed: Frontend E2E (1/8) failed on tests/chat-code-workflow.spec.ts (both the main "repo chat hands an exact changed file..." test and the "resized desktop Chromium pins theme..." test), expecting the Code rail region to auto-open on a fresh 0->N edit batch. Root-caused: use-workspace-rail-controller.ts passes autoRevealChanges:false unconditionally -- this branch's deliberate design: a discoverable CodeRailReopen pull-tab cue replaces auto-open, confirmed correct/intentional by the already-scoped tests/code-rail.spec.ts, which explicitly asserts the rail stays closed and only the reopen cue updates on new edits. The E2E spec was stale/pre-dating this design. Fixed by updating both assertions in tests/chat-code-workflow.spec.ts to click the "Show code rail" reopen button before expecting the rail region visible. Verified locally: lint pass, typecheck pass, both previously-failing tests pass (6/6 in the file, desktop project, via alternate port 3187 to avoid colliding with a concurrent worktree's dev server on 3100).

New head: 401c1546b9a642d3b7202fcfb5ef70b343489142 (pushed, PR #5355 headRefOid confirmed matching). Polling CI for the required Frontend build check next; will report to parent (cave-5zfxv) once resolved. Still in_progress, not merging per instructions.
