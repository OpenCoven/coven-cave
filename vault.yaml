# Cave starts without configured credentials or password-manager references.
# Add your own environment variables in Secret Vault. Values entered there are
# encrypted locally; this file contains only the storage metadata you save.
# Existing launcher variables and a user-configured .env.local remain usable.
# Never ship personal provider references as application defaults.
{}
