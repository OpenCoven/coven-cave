<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	Merged into the generated macOS bundle Info.plist by tauri-bundler.
	Voice calls capture the microphone (WebView getUserMedia for cloud
	providers; AVAudioEngine in src-tauri/src/speech.rs for the native
	local engine) and transcribe with SFSpeechRecognizer — macOS kills
	the process on first use of either without these usage descriptions.
	-->
	<!--
	The bundle file stays CovenCave.app — renaming it would put a space in
	every DMG, MSI and AppImage filename and break the release scripts that
	construct those names. This key is the user-visible name only: Finder,
	the menu bar, and the process name Discord reads for its detected-activity
	entry, which is a separate surface from the Rich Presence card in
	src-tauri/src/discord_presence.rs.
	-->
	<key>CFBundleDisplayName</key>
	<string>Coven Cave</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>Coven Cave uses the microphone for voice calls with your familiars.</string>
	<key>NSSpeechRecognitionUsageDescription</key>
	<string>Coven Cave transcribes your voice on this Mac so local voice calls work without any cloud service.</string>
</dict>
</plist>
